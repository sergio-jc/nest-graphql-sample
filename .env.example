# Copia a .env si quieres sobreescribir valores por defecto

# ---------------------------------------------------------------------------
# Base de datos
# ---------------------------------------------------------------------------

# Desarrollo local (SQLite via libSQL)
DATABASE_URL="file:./prisma/dev.db"

# Producción con Turso — reemplaza con tus valores reales
# DATABASE_URL="libsql://<db-name>.turso.io"
# DATABASE_AUTH_TOKEN="<tu-auth-token>"

# ---------------------------------------------------------------------------
# OpenTelemetry
# ---------------------------------------------------------------------------
OTEL_SERVICE_NAME=nest-graphql-sample
# Endpoint OTLP HTTP de trazas
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://localhost:4318/v1/traces
# Endpoint OTLP HTTP de logs
OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=http://localhost:4318/v1/logs
# Desactiva el SDK (útil para depurar sin enviar trazas)
# OTEL_SDK_DISABLED=true

# ---------------------------------------------------------------------------
# API Key interna — apps de test que la envíen en X-API-Key saltan el throttle
# ---------------------------------------------------------------------------
# INTERNAL_API_KEY="cambia-esto-por-un-valor-secreto"
